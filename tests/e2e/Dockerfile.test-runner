FROM python:3.12-slim

WORKDIR /app

# Copy the full repo (aegis-shield + aegis-monitor + tests)
COPY . /app

# Install aegis-shield with e2e deps
RUN pip install --no-cache-dir ".[e2e]"

CMD ["pytest", "tests/e2e/", "-v", "--tb=short"]
